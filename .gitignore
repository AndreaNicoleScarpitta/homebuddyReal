node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Dev-tool and Replit-specific artifacts — do not commit
.claude/
.config/
.local/
.replit
replit.nix

# Secrets — never commit
.env
.env.*
!.env.example
*.pem
*-service-account*.json
gcp-*.json

# Local dev helper scripts that may contain real credentials
start-local.bat
start-local.sh
